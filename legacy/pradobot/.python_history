import Jetson.GPIO as GPIO
GPIO.JETSON_INFO
GPIO.VERSION
GPIO.setmode(GPIO.BCM)
GPIO.setup(13, GPIO.IN)
GPIO.input(13)
exit()
import Jetson.GPIO as GPIO
GPIO.setmode(GPIO.BCM)
GPIO.input(11)
GPIO.setup(11, GPIO.IN)
GPIO.input(11)
GPIO.setmode(GPIO.BCM)
GPIO.setup(11, GPIO.IN)
GPIO.input(11)
GPIO.setup(11, GPIO.IN)
clear
exit()
import Jetson.GPIO as GPIO
GPIO.cleanup()
exit()
